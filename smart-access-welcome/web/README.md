# Web 欢迎页

欢迎页目前以 UTF-8 静态资源的形式内置在 `modules/access-server`，不需要 Node.js。主程序默认在 `http://127.0.0.1:8080/` 提供页面；仿真程序会在 `8080` 至 `8089` 中选择空闲端口，并在桌面界面显示实际地址。

浏览器通过同源 `/ws` 接收通过、拒绝和终端状态事件，断线重连后从 `/api/v1/terminal/status` 获取当前状态。

本目录留作以后拆分前端资源使用。若进行拆分，应继续避免第三方 CDN、人员管理功能和生物特征数据，并补充浏览器端自动化测试。
